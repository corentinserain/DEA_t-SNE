import matplotlib.pyplot as plt
import sklearn
from sklearn.datasets import make_blobs
from sklearn.datasets import make_classification
import seaborn as sns
import pandas as pd

# data, labels = sklearn.datasets.make_blobs(n_samples=300, n_features=2,
#                              centers=None, 
#                              cluster_std=4.0, 
#                              center_box=(-10.0, 10.0), 
#                              shuffle=True,
#                              random_state=None, 
#                              return_centers=False)

# plt.scatter(data[:, 0], data[:, 1], c=labels, cmap='viridis', edgecolors='k')
# plt.title('Generated Blobs')
# plt.xlabel('Feature 1')
# plt.ylabel('Feature 2')
# plt.show()

features, labels = make_classification(
                    n_samples=100,
                    n_features=5,
                    n_informative=2,
                    n_redundant=2,
                    n_classes=1,
                    n_clusters_per_class=2,
                    flip_y=0.01,
                    class_sep=1.0,
                    hypercube=True,
                    shift=0.0,
                    scale=1.0,
                    shuffle=True,
                    random_state=None
                )
''''''
# columns = [f'Feature_{i}' for i in range(features.shape[1])]
# df = pd.DataFrame(features, columns=columns)
# df['Target'] = labels

# print(df.head())

# sns.pairplot(df, hue='Target', diag_kind='kde')
# plt.show()

